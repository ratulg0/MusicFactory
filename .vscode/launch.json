{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "chrome",
            "request": "launch",
            "name": "Run Website in Chrome (no server needed)",
            "file": "${workspaceFolder}/index.html"
        },
        {
            "type": "msedge",
            "request": "launch",
            "name": "Run Website in Edge (no server needed)",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}
