# 56 Event Handling Standards

Use addEventListener. Avoid inline handlers.

Prefer event delegation for repeated elements.

Remove listeners when components are destroyed.

Keep handlers short and call dedicated functions.
