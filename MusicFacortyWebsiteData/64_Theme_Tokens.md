# 64 Theme Tokens

Theme tokens map design decisions to CSS variables.

Primary Background Surface Card Accent Text Muted Text Border

Never reference raw hex values inside components. Always use semantic
tokens.

Acceptance Criteria - Single source of truth - Easy future rebranding -
Consistent appearance
